# Цифровая подпись  
**Фатеева Елизавета Артёмовна**, НПИмд-01-24  
Дисциплина: *Математические основы защиты информации и информационной безопасности*  
5 ноября 2025 г.

---

## Слайд 1. Обзор темы  
Цифровая подпись гарантирует:  
- **подлинность**,  
- **целостность**,  
- **юридическую силу** данных в цифровом мире.  

В условиях бурной цифровизации она защищает авторство и неизменность информации, обеспечивая соответствие **законодательству РФ** и **международным стандартам**.

---

## Слайд 2. Значение цифровой подписи  
В современном информационном пространстве цифровая подпись — ключевой инструмент для обеспечения доверия в электронных взаимодействиях.

---

## Слайд 3. Основные свойства  
### 🔹 Подлинность отправителя  
Подпись создаётся **закрытым ключом** и проверяется **открытым**, что подтверждает авторство и защищает от подделки.

### 🔹 Гарантия целостности данных  
Любое изменение в сообщении делает подпись **недействительной**.

### 🔹 Неотказуемость и правовая значимость  
Подписанный документ невозможно отвергнуть. Закреплено в:  
- **ISO/IEC 9796**,  
- **RFC 5751**,  
- **Федеральном законе №63-ФЗ**.

---

## Слайд 4. Различия между цифровой и электронной подписью  

| Вид | Описание | Юридическая сила |
|-----|----------|-------------------|
| **Простая ЭП** | Логин + пароль, без криптографии | ❌ Нет |
| **УНЭП** | Усиленная защита, но не соответствует всем требованиям УКЭП | ⚠️ Ограниченная |
| **УКЭП** | Основана на **цифровой подписи**, соответствует ФЗ‑63 | ✅ Полная |
| **Цифровая подпись** | Математическая основа УКЭП, реализует надёжную защиту | — |

---

## Слайд 5. Этапы формирования и проверки  
1. **Подписание**:  
   - Вычисляется хеш сообщения: $ H(m) $.  
   - Хеш шифруется закрытым ключом: $ \sigma = S_{sk}(H(m)) $.  

2. **Проверка**:  
   - Получатель вычисляет $ H(m) $.  
   - Расшифровывает подпись открытым ключом: $ V_{pk}(\sigma) $.  
   - Сравнивает:  
     $$
     H(m) \stackrel{?}{=} V_{pk}(\sigma)
     $$  
   → Совпадение гарантирует подлинность и целостность.

---

## Слайд 6. Математические принципы алгоритма RSA  
- Основан на **трудности факторизации** произведения больших простых чисел.  
- Ключевой этап: выбор простых $ p $ и $ q $, вычисление $ n = pq $.  
- Функция Эйлера: $ \phi(n) = (p-1)(q-1) $.  
- Закрытый ключ $ d $ — обратный к открытому $ e $ по модулю $ \phi(n) $:  
  $$
  ed \equiv 1 \pmod{\phi(n)}
  $$  
- Подпись:  
  $$
  \sigma = H(m)^d \bmod n
  $$  
- Проверка:  
  $$
  H(m) \stackrel{?}{=} \sigma^e \bmod n
  $$

---

## Слайд 7. Основы алгоритма DSA  
- Основан на **задаче дискретного логарифма**.  
- Параметры:  
  - простое $ p $,  
  - простое $ q \mid (p-1) $,  
  - генератор $ g = h^{(p-1)/q} \bmod p $.  
- Ключи:  
  - закрытый: $ x \in [1, q-1] $,  
  - открытый: $ y = g^x \bmod p $.  
- Подпись — пара $ (r, s) $:  
  $$
  r = (g^k \bmod p) \bmod q,\quad
  s = k^{-1}(H(m) + xr) \bmod q
  $$  
  где $ k $ — случайное число.  
- Проверка:  
  $$
  w = s^{-1} \bmod q,\quad
  u_1 = H(m)w \bmod q,\quad
  u_2 = rw \bmod q,\\
  v = (g^{u_1} y^{u_2} \bmod p) \bmod q,\quad
  v \stackrel{?}{=} r
  $$

---

## Слайд 8. ECDSA: криптография на эллиптических кривых  
- Работает с точками кривой:  
  $$
  y^2 = x^3 + ax + b,\quad 4a^3 + 27b^2 \ne 0
  $$  
  над конечным полем.  
- Базовая точка $ G $ порядка $ n $.  
- Ключи:  
  - закрытый: $ d \in [1, n-1] $,  
  - открытый: $ Q = dG $.  
- Подпись $ (r, s) $:  
  1. $ (x_1, y_1) = kG $,  
  2. $ r = x_1 \bmod n $,  
  3. $ s = k^{-1}(H(m) + dr) \bmod n $.  
- Проверка:  
  $$
  w = s^{-1} \bmod n,\quad
  u_1 = H(m)w \bmod n,\quad
  u_2 = rw \bmod n,\\
  (x_1, y_1) = u_1 G + u_2 Q,\quad
  r \stackrel{?}{=} x_1 \bmod n
  $$

---

## Слайд 9. Сравнение размеров ключей (NIST, 2025)  
| Алгоритм | Размер ключа для ~128-битной стойкости |
|----------|----------------------------------------|
| RSA      | 3072 бит |
| DSA      | 3072 бит |
| **ECDSA**| **256 бит** |

→ ECDSA обеспечивает ту же безопасность при **значительно меньших** размерах ключей → меньше нагрузка на ресурсы.

---

## Слайд 10. Роль в TLS/SSL  
- **Аутентификация сервера**:  
  Сервер предоставляет сертификат, подписанный CA (RSA/ECDSA).  
- **Защита обмена ключами**:  
  Подпись параметров сессии (например, в ECDHE) предотвращает MITM.  
- **Аутентификация клиента** (опционально):  
  Клиент также может подписывать запросы.

---

## Слайд 11. Использование в российских системах  
- **ФЗ‑63** и **ГОСТ Р 34.10–2012** — правовая и техническая основа.  
- ГОСТ — отечественный стандарт на эллиптических кривых (аналог ECDSA).  
- Применение:  
  - Госуслуги,  
  - Бухгалтерский учёт (СБИС, Контур),  
  - Судебная практика.

---

## Слайд 12. Роль ECDSA в криптовалютах и блокчейне  
- В **Bitcoin** ECDSA подписывает транзакции.  
- Владелец кошелька использует **закрытый ключ** для подписи.  
- Сеть проверяет подпись **открытым ключом**.  
- ⚠️ **Неустойчив к квантовым атакам** → стимулирует разработку **постквантовой криптографии**.

---

## Слайд 13. Преимущества цифровой подписи  
- Гарантия подлинности, целостности, неотказуемости.  
- Прозрачная верификация любым обладателем открытого ключа.  
- Поддержка стандартов: **PKI, X.509, CMS** → высокая совместимость.

---

## Слайд 14. Ограничения  
- Зависимость от сохранности **закрытого ключа**.  
- Нет встроенной защиты от **повтора (replay)** — требует nonce/меток времени.  
- Вычислительная сложность (особенно RSA).  
- Уязвимость к **квантовым атакам** (алгоритм Шора).  
- Требуется **инфраструктура доверия (PKI)**.

---

## Слайд 15. Заключение  
Цифровая подпись — фундамент безопасности цифрового века.  
Опирается на проверенные математические основы:  
- факторизацию (RSA),  
- дискретный логарифм (DSA),  
- эллиптические кривые (ECDSA, ГОСТ).  

Перспектива — переход к **постквантовым алгоритмам** (NIST, ГОСТ-ПК).
